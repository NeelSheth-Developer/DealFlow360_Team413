# Point this at the DealFlow360 backend to make the risk score (and any other
# server-authoritative calculation) come from the API instead of the local
# fallback mirror. Leave it blank to run fully offline on seed data.
VITE_API_BASE_URL=

# Optional request timeout in milliseconds. Defaults to 8000.
VITE_API_TIMEOUT_MS=8000
